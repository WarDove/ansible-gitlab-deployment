---
# tasks file for DNS-MANAGER

- name: DNS MANAGER
  become: yes
  command: grep {{ URL }} -s /etc/hosts 1>test1.txt || sh -c  "echo '{{ NGINX_SERVER }}  {{ URL }}' >> /etc/hosts"
  notify:
    - Reload dnsmasq
...
